import styled from 'styled-components';

// for styling the add another card div
export const Adder = styled.div`
  background-color: inherit;
  color: #00467f;
  font-weight: bold;
  opacity: 0.5;
  padding: 0.5rem;
  font-size: 0.9rem;
  cursor: pointer;
`;
